aivo -

```
Todo list as promised:

Fix duplicate chat messages
Fix explored/trail cells not always appearing when cells die
Fix locked bases being undiscoverable/undestroyable
Fix can't place cells outside outposts when main base is destroyed
Fix base not respawning when destroyed but outposts still exist

Mines: $100K, single hidden cell (for other players), births 4 adjacent cells when cell enters 5-cell radius
Main base under attack notification (flash when HP dropping)
Can't build outposts directly adjacent to other players' main bases
Map size to 2048×2048

Drag patterns to grid (click pattern name → shows ghost → release to drop → click send)
Arrow keys to shift pending cells before placing
Remove built-in pattern templates, keep only user-saved patterns
Make leaderboard, players online, upgrades, patterns, coords, chat text white
Increase coordinates text size
Hover over online count to see player names
Add X button to close/hide radio panel
Chat swear filter

Optimisation - if anyone is a programmer please let me know as this is the big one that will allow us to increase the map size

Set up GitHub/GitLab repo [DONE]
Persistence confirmed working (Railway volume at /data)
```
